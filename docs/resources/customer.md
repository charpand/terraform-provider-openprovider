---
page_title: "openprovider_customer Resource - terraform-provider-openprovider"
subcategory: ""
description: |-
  Manages an OpenProvider customer (contact handle).
---

# openprovider_customer (Resource)

Manages an OpenProvider customer (contact handle). Customer handles are used as contact information for domain registrations (owner, admin, tech, billing).

## Example Usage

### Basic Customer

```terraform
resource "openprovider_customer" "owner" {
  email = "owner@example.com"

  phone {
    country_code = "1"
    area_code    = "555"
    number       = "1234567"
  }

  address {
    street  = "Main Street"
    number  = "123"
    city    = "New York"
    state   = "NY"
    zipcode = "10001"
    country = "US"
  }

  name {
    first_name = "John"
    last_name  = "Doe"
  }
}
```

### Company Customer with Domain

```terraform
resource "openprovider_customer" "company_owner" {
  company_name = "Example Corp"
  email        = "contact@example.com"
  locale       = "en_US"
  comments     = "Primary domain owner for company domains"

  phone {
    country_code = "1"
    area_code    = "555"
    number       = "9876543"
  }

  address {
    street  = "Tech Boulevard"
    number  = "456"
    suffix  = "Suite 100"
    city    = "San Francisco"
    state   = "CA"
    zipcode = "94105"
    country = "US"
  }

  name {
    first_name = "Jane"
    last_name  = "Smith"
    prefix     = "Ms."
  }
}

# Use the customer handle in a domain resource
resource "openprovider_domain" "company_domain" {
  domain       = "example.com"
  owner_handle = openprovider_customer.company_owner.handle
  period       = 1
}
```

### Multiple Customers for Different Roles

```terraform
# Create multiple customers for different contact roles
resource "openprovider_customer" "owner" {
  email = "owner@example.com"

  phone {
    country_code = "1"
    area_code    = "555"
    number       = "1111111"
  }

  address {
    street  = "Main St"
    number  = "100"
    city    = "New York"
    country = "US"
  }

  name {
    first_name = "John"
    last_name  = "Owner"
  }
}

resource "openprovider_customer" "admin" {
  email = "admin@example.com"

  phone {
    country_code = "1"
    area_code    = "555"
    number       = "2222222"
  }

  address {
    street  = "Main St"
    number  = "100"
    city    = "New York"
    country = "US"
  }

  name {
    first_name = "Jane"
    last_name  = "Admin"
  }
}

resource "openprovider_customer" "tech" {
  email = "tech@example.com"

  phone {
    country_code = "1"
    area_code    = "555"
    number       = "3333333"
  }

  address {
    street  = "Main St"
    number  = "100"
    city    = "New York"
    country = "US"
  }

  name {
    first_name = "Bob"
    last_name  = "Tech"
  }
}

# Use all three customer handles in a domain
resource "openprovider_domain" "example" {
  domain         = "example.com"
  owner_handle   = openprovider_customer.owner.handle
  admin_handle   = openprovider_customer.admin.handle
  tech_handle    = openprovider_customer.tech.handle
  billing_handle = openprovider_customer.admin.handle
  period         = 1
  autorenew      = true
}
```

<!-- schema generated by tfplugindocs -->
## Schema

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `email` (String) The customer's email address.

### Optional

- `address` (Block, Optional) The customer's address. (see [below for nested schema](#nestedblock--address))
- `comments` (String) Custom notes about this customer.
- `company_name` (String) The company name (optional).
- `locale` (String) The customer's language/locale (e.g., en_US).
- `name` (Block, Optional) The customer's name. (see [below for nested schema](#nestedblock--name))
- `phone` (Block, Optional) The customer's phone number. (see [below for nested schema](#nestedblock--phone))

### Read-Only

- `handle` (String) The customer handle (e.g., XX123456-XX). This is auto-generated by Openprovider upon creation.
- `id` (String) The customer identifier (same as handle).

<a id="nestedblock--address"></a>
### Nested Schema for `address`

Required:

- `city` (String) City name.
- `country` (String) Country code (e.g., US, NL).
- `street` (String) Street name.

Optional:

- `number` (String) Street number.
- `state` (String) State or province.
- `suffix` (String) Address suffix (e.g., apartment number).
- `zipcode` (String) Postal/ZIP code.


<a id="nestedblock--name"></a>
### Nested Schema for `name`

Required:

- `first_name` (String) First name.
- `last_name` (String) Last name.

Optional:

- `initials` (String) Initials.
- `prefix` (String) Name prefix (e.g., Mr., Dr.).


<a id="nestedblock--phone"></a>
### Nested Schema for `phone`

Required:

- `area_code` (String) Area code.
- `country_code` (String) Country code (e.g., 1 for US).
- `number` (String) Phone number.




## Import

Import a customer using the customer handle.

```shell
# Import an existing customer handle
terraform import openprovider_customer.my_customer XX123456-XX
```
